import { createTheme } from "@mui/material";

export const theme = createTheme({
  palette:{
    primary:{
      main: "#0FEF00",
      light: "skyblue",
    },
    secondary:{
      main: '#C200F0',
    },
    otherColor:{
      main:"#999"
    }
  }
})